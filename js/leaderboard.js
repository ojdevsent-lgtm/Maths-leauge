import { supabase, watchAuth } from "./supabase.js";
const rankingList=document.getElementById("rankingList"),topThree=document.getElementById("topThree"),message=document.getElementById("leaderboardMessage"),status=document.getElementById("rankingStatus");const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;","\"":"&quot;"}[c]));
async function load(mode="overall"){if(status)status.textContent=mode[0].toUpperCase()+mode.slice(1);if(rankingList)rankingList.innerHTML='<div class="leaderboard-loading">Loading rankings...</div>';try{const{data,error}=await supabase.rpc("get_leaderboard",{p_mode:mode});if(error)throw error;const rows=data||[];if(!rows.length){topThree.innerHTML="";rankingList.innerHTML='<div class="leaderboard-empty">No rankings yet.</div>';return;}topThree.innerHTML=rows.slice(0,3).map(r=>`<div class="top-player"><span class="ranking-number">#${r.rank}</span><span class="ranking-name">${esc(r.full_name)}</span><span class="ranking-points"><strong>${r.points}</strong><span>pts</span></span></div>`).join("");rankingList.innerHTML=rows.map(r=>`<div class="ranking-row"><span class="ranking-number">#${r.rank}</span><div><span class="ranking-name">${esc(r.full_name)}</span><span class="ranking-id">${esc(r.registration_number)}</span></div><div class="ranking-points"><strong>${r.points}</strong><span>points</span></div></div>`).join("");}catch(error){console.error(error);if(message){message.textContent="Unable to load the leaderboard.";message.classList.add("show");}}}
document.querySelectorAll(".leaderboard-tab").forEach(tab=>tab.addEventListener("click",()=>{document.querySelectorAll(".leaderboard-tab").forEach(t=>t.classList.remove("active"));tab.classList.add("active");load(tab.dataset.ranking||"overall");}));watchAuth(user=>{if(!user)location.href="auth.html";else load("overall");});

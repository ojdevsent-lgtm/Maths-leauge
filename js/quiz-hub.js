import { supabase } from "./supabase.js";
const dailyQuiz=document.getElementById("dailyQuiz"),dailyStatus=document.getElementById("dailyStatus"),quizMessage=document.getElementById("quizMessage");
const message=t=>{if(quizMessage){quizMessage.textContent=t;quizMessage.classList.add("show");}};
let currentUser=null;
supabase.auth.getUser().then(async({data})=>{currentUser=data.user;if(!currentUser){dailyStatus.textContent="Login Required";dailyQuiz?.setAttribute("aria-disabled","true");return;}try{const{data:q,error}=await supabase.rpc("get_daily_quiz");if(error)throw error;dailyQuiz.dataset.completed=q.completed?"true":"false";dailyQuiz.dataset.active=q.questions?.length?"true":"false";dailyStatus.textContent=q.completed?"Already Taken":q.questions?.length?"Available":"Unavailable";dailyStatus.classList.toggle("available",!q.completed&&!!q.questions?.length);}catch(e){console.error(e);message("We couldn't check your quiz status. Please try again.");}});
dailyQuiz?.addEventListener("click",()=>{if(!currentUser)return location.href="auth.html";if(dailyQuiz.dataset.active==="false")return message("Today's Daily Quiz is currently unavailable.");if(dailyQuiz.dataset.completed==="true")return message("Quiz already taken. You have already completed today's Daily Quiz.");location.href="take-quiz.html";});

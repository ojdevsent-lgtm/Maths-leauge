import { supabase, watchAuth } from "./supabase.js";
const $=id=>document.getElementById(id); const set=(id,v)=>{const e=$(id);if(e)e.textContent=v;}; const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;","\"":"&quot;"}[c]));
watchAuth(async user=>{if(!user)return location.replace("auth.html");try{const{data,error}=await supabase.rpc("get_student_dashboard");if(error)throw error;const s=data.student||{},stats=data.stats||{};set("studentName",(s.fullName||"Student").trim().split(" ")[0]);set("registrationNumber",s.registrationNumber||"Not assigned");set("totalPoints",stats.totalPoints??0);set("quizzesCompleted",stats.quizzesCompleted??0);set("averageScore",`${stats.averageScore??0}%`);set("leagueRank",stats.leagueRank?`#${stats.leagueRank}`:"—");const announcements=$("announcementsList");if(announcements){announcements.innerHTML=(data.announcements||[]).map(a=>`<article class="dashboard-announcement"><strong>${esc(a.title)}</strong><p>${esc(a.body)}</p></article>`).join("");announcements.closest(".dashboard-section")?.classList.toggle("hidden",!(data.announcements||[]).length);}const list=$("activityList"),empty=$("activityEmpty"),attempts=data.attempts||data.recentAttempts||[];if(attempts.length){empty&&(empty.style.display="none");list&&(list.innerHTML=attempts.map(a=>`<div class="activity-item"><div class="activity-item-left"><div class="activity-item-icon"><i class="ri-brain-line"></i></div><div><strong>${esc(a.quizTitle||"Daily Quiz")}</strong><span>${a.score}/${a.totalQuestions} · ${a.points} points</span></div></div></div>`).join(""));}else{empty&&(empty.style.display="block");list&&(list.innerHTML="");}const{data:admin}=await supabase.rpc("is_admin");const adminLink=$("adminLink");if(adminLink)adminLink.hidden=!admin;}catch(error){console.error(error);$("dashboardError")&&( $("dashboardError").textContent="Unable to load your dashboard. Please refresh and try again." );}});
document.querySelectorAll(".nav-item[data-page]").forEach(item=>item.addEventListener("click",()=>{const routes={quiz:"quiz.html",rank:"leaderboard.html",progress:"progress.html",profile:"profile.html"};if(routes[item.dataset.page])location.href=routes[item.dataset.page];}));
$("quizAction")?.addEventListener("click",()=>location.href="quiz.html");$("progressAction")?.addEventListener("click",()=>location.href="progress.html");$("startFirstQuiz")?.addEventListener("click",()=>location.href="quiz.html");$("viewAllActivity")?.addEventListener("click",()=>location.href="progress.html");
